# Local AI agent, editor, and GitHub CLI config (if present)
.claude
.codex
.config/gh

# composer
composer.lock

# GitHub Copilot config (if present)
.github/agents/**
.github/copilot-instructions.md
.github/copilot/**
.github/instructions/**
.github/prompts/**
.github/skills/**

# Internal docs
internal

# mac ds_store (if present)
.DS_Store

# node_modules (if present)
node_modules

# phpstorm project (if present)
.idea

# phpactor (if present)
.phpactor.*

# phpunit (if present)
.phpunit.cache
.phpunit.result.cache

# mutation testing
.stryker-tmp
reports/mutation

# local browser quality artifacts
artifacts

# vendor
vendor

# vscode project (if present)
.vscode

# windows thumbnail cache (if present)
Thumbs.db
